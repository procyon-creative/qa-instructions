name: E2E

# Deterministic fixture-site capture/render + golden verification (main only).
on:
  push:
    branches: [main]
    paths-ignore: ['**/*.md', 'docs/**']

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - run: pnpm exec playwright install chromium
        working-directory: examples/verification
      - run: pnpm e2e
        working-directory: examples/verification

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: qa-steps
          path: examples/verification/qa-steps-out/

      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: |
            examples/verification/playwright-report/
            examples/verification/test-results/
          retention-days: 7
